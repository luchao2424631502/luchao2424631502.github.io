---
title: LDD(linux设备驱动)环境搭建
date: '2020-12-24 11:18:00'
permalink: 2020/12/24/LDD-linux设备驱动-环境搭建/
categories:
- 系统与底层
- 驱动与嵌入式
tags:
- 驱动
---

# LDD(linux设备驱动程序)环境搭建

> 2020/12/24 10:08

1. vbox下安装ubuntu5.04 (i386) [iso下载地址](http://old-releases.ubuntu.com/releases/)
2. 安装gcc `sudo apt-get install gcc` (应该是本地源吧 `gcc version 3.3.5`)
3. 将下载的linux2.6.10[源码](https://www.kernel.org/pub/linux/kernel/v2.6/linux-2.6.10.tar.gz) `tar -xzvf xx`解压到/usr/src目录,然后进入此目录

   ```makefile
make oldconfig
make bzImage
make modules
make modules_install
```
4. 制作initrd.img文件

   `mkinitrd -o /boot/initrd.img-2.6.10 2.6.10`

   2.6.10表示的是`/lib/modules`下的目录
5. `sudo update-grub`
6. 重启F12选2.6.10

---

- 随便选一个目录放测试模块代码,

  hello.c

  ```c
#include <linux/init.h>
#include <linux/module.h>
MODULE_LICENSE("GPL");

static int hello_init(void)
{
	printk(KERN_ALERT"hello,world\n");
	return 0;
}

static void hello_exit(void)
{
	printk(KERN_ALERT"exit\n");
}

module_init(hello_init);
module_exit(hello_exit);
```
- Makefile

  ```makefile
obj-m := hello.o
KDIR := /lib/modules/2.6.10/build

all:
	make -C $(KDIR) M=$(PWD) modules
	
clean:
	rm -f *.ko *.o *.mod.o *.mod.c *.symvers
```

  `obj-m`编译可装载的模块,`obj-y`是编译进内核

  `KDIR`指定内核目录树的路径,`-C`是make的更换目录选项

  `M`

  ![1](/images/Selection015.png)

  使用`M=dir`指定要构建的外部模块的目录(就是hello.c的目录)

  `make modules`表示编译模块

---

1. 在hello.c目录下执行make
2. sudo提权
3. `insmod hello.ko`加载模块
4. `rmmod hello`卸载模块
5. `dmesg`查看日志消息(如果控制台没有)

   ![2](/images/Selection016.png)

---

[参考1](https://blog.csdn.net/u013162593/article/details/45252383?utm_medium=distribute.pc_relevant.none-task-blog-baidujs_title-2&spm=1001.2101.3001.4242)

[参考2](https://blog.csdn.net/ypoflyer/article/details/6087668?utm_medium=distribute.pc_relevant.none-task-blog-BlogCommendFromMachineLearnPai2-1.control&depth_1-utm_source=distribute.pc_relevant.none-task-blog-BlogCommendFromMachineLearnPai2-1.control)
