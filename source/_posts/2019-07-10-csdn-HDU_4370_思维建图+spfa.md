---
title: HDU_4370_思维建图+spfa
date: '2019-07-10 11:02:00'
permalink: 2019/07/10/csdn/HDU_4370_思维建图+spfa/
categories:
- 算法与数据结构
- 图论
---

[HDU 4370](http://acm.hdu.edu.cn/showproblem.php?pid=4370)

- 思维题目,将条件转化成最短路来做,观察题目条件.
- 求$\Sigma c\_{ij}\*x\_{ij}$,等价于常数乘以边的权值,恰恰这些常数是只能是`0/1`,所以只能是边的组合.
- 条件1：起始点`1`到其余**每个点中的任意一个点**可以**有一条边**,出度为1.
- 条件2：**其余每一个点中的**中**任意一个点**到终点`4`(`n`)可以**有一条边**,入度为1.
- 条件3：观察下标发现`2 - 3`之间的点到源点和终点常数都相同,说明`2 - n-1`之间的点**入度和出度**相同
- $\Sigma c\_{ij}\*x\_{ij}$,不仅包括了`1`到`n`的最短路,还有`1`到`1`的环,`n`到`n`的环,因为入/出度为1.而中间的点入/出度只要求相同,可以为0.
  ```cpp
#include <iostream>
#include <string.h>
#include <queue>
using namespace std;
const int maxn = 305;
int n;
int e[maxn][maxn];
bool vis[maxn];
int dist[maxn];

void spfa(int x,int& ans1) {
	queue<int> que;
	memset(dist,0x3f,sizeof dist);
	memset(vis,0,sizeof vis);
	dist[x] = 0; vis[x] = 1; que.push(x);
	while (!que.empty()) {
		int cur = que.front(); que.pop();
		vis[cur] = 0;
		for (int i=1; i<=n; i++) {
			if (dist[i] > dist[cur] + e[cur][i]) {
				dist[i] = dist[cur] + e[cur][i];
				if (!vis[i]) {
					vis[i] = 1;
					que.push(i);
				}
			}
			// 更新起始点环的距离
			if (cur != x) ans1 = min(ans1,dist[cur]+e[cur][x]);
		}
	}
}

int main() {
//	freopen("a.txt","r",stdin);
	while (scanf("%d",&n) != EOF) {
		memset(e,0x3f,sizeof e);
		for (int i=1; i<=n; i++) 
			for (int j=1; j<=n; j++) 
				scanf("%d",&e[i][j]);
		int ans1=0x3f3f3f3f,ans2=0x3f3f3f3f;
		spfa(1,ans1); 
		int ans = dist[n];
		spfa(n,ans2);
		cout << min(ans,ans1+ans2) << endl;
	}
	return 0;
}
```
